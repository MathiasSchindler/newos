# PGPMSG

## NAME

pgpmsg - inspect OpenPGP messages and expose message crypto commands

## SYNOPSIS

```
pgpmsg [--json] COMMAND [ARGS...]

pgpmsg inspect [FILE]
pgpmsg verify [-k PUBRING] SIGNATURE [FILE]
pgpmsg encrypt -r RECIPIENT [-r RECIPIENT ...] [-k PUBRING] [-o OUT] [--armor] [--compress=ALG] [--stream] [--DANGER-anyway] [FILE]
pgpmsg decrypt [-s SECRING] [-o OUT] [FILE]
pgpmsg sign -u SIGNER [-s SECRING] [-o OUT] [--armor] [--detach|--cleartext] [--DANGER-anyway] [FILE]
```

## DESCRIPTION

`pgpmsg` is the OpenPGP message companion to `pgpkey`. `pgpkey` manages and
inspects public certificates and local public keyrings; `pgpmsg` is the place for
message and file operations such as encryption, decryption, signing, and
signature verification.

The current implementation provides packet inspection, signature packet metadata
reporting, Ed25519 detached binary and cleartext signatures, and a generated-key
encryption path using X25519/ECDH subkeys with AES-256 encrypted session keys and
integrity-protected OpenPGP literal or compressed data packets. Binary
uncompressed encryption can also write OpenPGP partial-body-length packets with
`--stream`. Unsupported algorithms and message shapes fail explicitly instead of
producing non-OpenPGP output or making trust claims.

## COMMANDS

- `inspect [FILE]` - decode binary or ASCII-armored OpenPGP input and list packet tags and body lengths. With no FILE or `-`, read standard input.
- `verify [-k PUBRING] SIGNATURE [FILE]` - decode SIGNATURE and report signature packet metadata. With FILE and a matching Ed25519 public key in PUBRING, verify a detached binary signature cryptographically. Without FILE, print metadata only.
- `encrypt -r RECIPIENT [-r RECIPIENT ...] [-k PUBRING] [-o OUT] [--armor] [--compress=ALG] [--stream] [--DANGER-anyway] [FILE]` - encrypt FILE, or standard input, to one or more matching X25519/ECDH encryption subkeys in PUBRING. Each RECIPIENT may match the primary certificate fingerprint, an encryption subkey fingerprint, or a user ID substring.
- `decrypt [-s SECRING] [-o OUT] [FILE]` - decrypt an OpenPGP message produced by this tool using a matching unprotected X25519 secret subkey from SECRING.
- `sign -u SIGNER [-s SECRING] [-o OUT] [--armor] --detach [FILE]` - create an Ed25519 detached binary signature using an unprotected secret key generated by `pgpkey generate`.
- `sign -u SIGNER [-s SECRING] -o OUT --cleartext [FILE]` - create an OpenPGP cleartext signed message with a canonical text signature and an armored signature block.

## OPTIONS

- `--json` - emit shared JSON Lines events where supported.
- `-k PUBRING`, `--keyring PUBRING` - public keyring path for verification and encryption.
- `-s SECRING`, `--secring SECRING` - secret keyring path for signing and decryption.
- `-r RECIPIENT`, `--recipient RECIPIENT` - recipient selector for encryption. Repeat this option to encrypt the same message to multiple recipients.
- `-u SIGNER`, `--user SIGNER`, `--signer SIGNER` - signer selector for detached signatures.
- `-o OUT`, `--output OUT` - output path for message-writing commands.
- `--armor` - request ASCII-armored output for signatures and encrypted messages.
- `--stream` - for encryption, write binary unarmored uncompressed output using OpenPGP partial body lengths. This keeps plaintext input streaming through fixed-size chunks instead of reading the complete input before encryption. It currently cannot be combined with `--armor` or compression.
- `--detach` - request a detached signature.
- `--cleartext` - request a cleartext signed message. Cleartext output is always armored and requires `-o OUT`.
- `--compress=ALG`, `--compress ALG` - compress literal data before encryption. `ALG` may be `none` or `zlib`; ZIP-compressed messages can be decrypted, but ZIP compression is not emitted yet because the shared compression layer currently exports only zlib-wrapped compression. BZip2 compressed packets are diagnosed as unsupported.
- `--DANGER-anyway` - bypass signing/encryption key-usage checks. This is intended only for inspecting broken or experimental certificates.
- `-h`, `--help` - show usage.

## VERIFY OUTPUT

`verify` prints the signature packet type, public-key algorithm, hash algorithm,
signature creation date when present, issuer key ID, issuer fingerprint when
present, and `trust: not evaluated`. With detached data and a matching Ed25519
public key, the status is `good` or `bad` and the exit status reflects the
cryptographic result. Metadata-only verification remains `not checked`.

## SIGN OUTPUT

`sign --detach` writes a binary OpenPGP signature packet by default. With
`--armor`, it writes an ASCII-armored `PGP SIGNATURE` block. `sign --cleartext`
writes a `PGP SIGNED MESSAGE` wrapper, dash-escapes cleartext lines, canonicalizes
line endings for the signature hash, and appends an armored signature block. The
supported signing path is Ed25519 with SHA-512. Inline signed messages,
passphrase-protected secret keys, and encrypted secret-key packets are not
implemented yet.

## ENCRYPTION OUTPUT

`encrypt` writes one public-key encrypted session key packet per recipient,
followed by a version 1 symmetrically encrypted integrity-protected data packet.
`decrypt` scans all supported recipient packets and uses the first one matching
the supplied secret keyring. The supported key path is the modern key shape
emitted by `pgpkey generate`: an Ed25519 signing primary key with an X25519/ECDH
encryption subkey. Encryption and signing enforce OpenPGP key flags by default:
signing requires a signing-capable primary key, and encryption requires a
non-revoked subkey with encryption flags. Pass `--DANGER-anyway` to bypass that
policy for test material.

Message payloads are wrapped as binary literal data packets, optionally inside a
ZLIB compressed data packet, and protected with the OpenPGP SHA-1 modification
detection code used by SEIPD v1 packets. Decryption accepts uncompressed, ZLIB,
and ZIP compressed payloads. BZip2 packets, AEAD packets, passphrase-protected
secret keys, and arbitrary third-party secret-key formats are not implemented
yet. RFC 9580 SEIPD v2 AEAD modes are not emitted or consumed yet, so encrypted
messages use the older CFB+MDC construction for current v4 interoperability.

With `--stream`, encryption emits the SEIPD packet and nested literal data packet
with OpenPGP partial body lengths. This mode is binary-only and currently limited
to `--compress=none`; ASCII-armored streaming, compressed streaming, and streaming
decryption are still future work. Inspect and decrypt normalize partial packets
after decoding so existing packet readers can process streamed messages, but those
read paths still coalesce packet bodies in memory.

Without `--stream`, encryption still reads the complete input and encrypted packet
into memory before writing output. It avoids permanent heap growth and bounds
inflate output, but does not use partial-body-length packet output.

## JSON Output

`inspect --json` emits `packet` events. `verify --json` emits `signature` events
with `status: good`, `bad`, or `not_checked` plus decoded signature metadata.

## EXIT STATUS

`0` is used for successful inspection, signing, encryption, decryption, and good
detached signature verification. `1` is used for syntax errors, input errors,
bad signatures, and unsupported or malformed encrypted messages. `2` is used
when a command shape is recognized but the required cryptographic operation is
not implemented yet.

## SEE ALSO

pgpkey, pgpquery
